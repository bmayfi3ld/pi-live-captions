replays/*
transcripts/*
.env
# Anchored: this is the event logo dropped in the repo root, not every file
# named logo.png. Unanchored, it also swallowed internal/web/testdata/logo.png
# and the web tests failed on any fresh clone.
/logo.png
bin/*
# Keyterm lists are per-event material, not code: they get edited on the day.
keyterms-*.txt
